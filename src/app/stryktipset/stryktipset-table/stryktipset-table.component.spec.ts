/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { StryktipsetTableComponent } from './stryktipset-table.component';

describe('Component: StryktipsetTable', () => {
  it('should create an instance', () => {
    let component = new StryktipsetTableComponent();
    expect(component).toBeTruthy();
  });
});
