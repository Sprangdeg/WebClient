/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { StudentOrganizerComponent } from './student-organizer.component';

describe('Component: StudentOrganizer', () => {
  it('should create an instance', () => {
    let component = new StudentOrganizerComponent();
    expect(component).toBeTruthy();
  });
});
